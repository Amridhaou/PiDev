<form method="post" action="{{ path('certificat_delete', {'Num_Certificat': certificat.NumCertificat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certificat.NumCertificat) }}">
    <button class="mt-2 btn bg-danger"> <h4 class="text-white"><i class="bi-x"> </i> Supprimer</h4></button>
</form>

