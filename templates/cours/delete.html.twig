<form method="post" action="{{ path('cours_del', {'CodeCours': cour.codeCours}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.codeCours) }}">
    <button class="mt-2 btn bg-danger"> <h4 class="text-white"><i class="bi-x"> </i> Supprimer</h4></button>
</form>
